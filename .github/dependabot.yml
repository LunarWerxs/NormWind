version: 2
updates:
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
    groups:
      routine-runtime-updates:
        applies-to: version-updates
        patterns:
          - "*"
        exclude-patterns:
          # Tailwind changes the canonical snapshot and always needs its own
          # compatibility/regeneration pass.
          - tailwindcss
        update-types:
          - minor
          - patch
    ignore:
      # Babel 8 requires Node >=22.18. NormWind's floor is >=22 since 2026-09-10, so
      # this hold is now about the 22.0-vs-22.18 gap only and is worth re-testing.
      - dependency-name: "@babel/parser"
        update-types:
          - version-update:semver-major
      # v4 removed the internal group-data export NormWind currently uses.
      - dependency-name: eslint-plugin-tailwindcss
        update-types:
          - version-update:semver-major

  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
