==== input.tsx (4 findings) ====
import React from "react";
import { createElement as vueCreateElement, h as hyperscript } from "vue";

declare const dynamic: string;
declare function track(value: unknown): void;

const unrelatedData = { class: "px-4 py-4" };
const serializedHtml = '<div class="mt-2 mb-2">serialized</div>';
const serializedTemplate = `<div className="w-4 h-4">serialized</div>`;
const serializedPattern = /class="pl-2 pr-2"/;

// <div className="pt-4 pb-4">comment</div>
track({ class: "ml-3 mr-3" });

export const Hyperscript = hyperscript("div", { class: "px-4 py-4" });
                                                        ~~~~ ~~~~
!!! tailwindcss/enforces-shorthand: Classnames 'px-4, py-4' could be replaced by the 'p-4' shorthand!
export const ReactElement = React.createElement("div", { className: "mt-2 mb-2" });
                                                                     ~~~~ ~~~~
!!! tailwindcss/enforces-shorthand: Classnames 'mt-2, mb-2' could be replaced by the 'my-2' shorthand!
export const ImportedAlias = vueCreateElement("div", {
    class: `w-4 h-4 ${dynamic}`,
            ~~~ ~~~
!!! tailwindcss/enforces-shorthand: Classnames 'w-4, h-4' could be replaced by the 'size-4' shorthand!
});
export const JsxElement = <div className="pl-2 pr-2">rendered</div>;
                                          ~~~~ ~~~~
!!! tailwindcss/enforces-shorthand: Classnames 'pl-2, pr-2' could be replaced by the 'px-2' shorthand!

void unrelatedData;
void serializedHtml;
void serializedTemplate;
void serializedPattern;

=== FIXED (--fix) ===
(unchanged)
=== FIXED (--fixall) ===
import React from "react";
import { createElement as vueCreateElement, h as hyperscript } from "vue";

declare const dynamic: string;
declare function track(value: unknown): void;

const unrelatedData = { class: "px-4 py-4" };
const serializedHtml = '<div class="mt-2 mb-2">serialized</div>';
const serializedTemplate = `<div className="w-4 h-4">serialized</div>`;
const serializedPattern = /class="pl-2 pr-2"/;

// <div className="pt-4 pb-4">comment</div>
track({ class: "ml-3 mr-3" });

export const Hyperscript = hyperscript("div", { class: "p-4" });
export const ReactElement = React.createElement("div", { className: "my-2" });
export const ImportedAlias = vueCreateElement("div", {
    class: `size-4 ${dynamic}`,
});
export const JsxElement = <div className="px-2">rendered</div>;

void unrelatedData;
void serializedHtml;
void serializedTemplate;
void serializedPattern;
